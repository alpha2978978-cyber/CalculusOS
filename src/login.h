#ifndef CALCULUSOS_LOGIN_H
#define CALCULUSOS_LOGIN_H

/* Draws the login screen, reads username/password from the
 * keyboard driver, and loops until the credentials are correct.
 * Never returns until login succeeds. */
void login_run(void);

#endif
