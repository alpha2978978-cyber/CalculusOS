#ifndef CALCULUSOS_STRING_H
#define CALCULUSOS_STRING_H
#include <stddef.h>

size_t k_strlen(const char *s);
int    k_strcmp(const char *a, const char *b);
void   k_memset(void *dst, int val, size_t n);

#endif
